import { Layout } from "@/modules/common/views/layout.js";

interface Props {}

export const Index = ({}: Props = {}) => {
  return (
    <Layout title="Home page">
      <h1>Hello world!</h1>
    </Layout>
  );
};
